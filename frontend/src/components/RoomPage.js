import React from 'react';
import { Container, Typography } from '@mui/material'

function RoomPage() {
    return (
        <Container>
            <Typography>
                You are in room.
            </Typography>
        </Container>
    );
}

export default RoomPage;